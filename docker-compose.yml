version: "3.9"
services:
  agent:
    build: .
    image: research-agent
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - agent_data:/data
    command: ["api"]

volumes:
  agent_data: